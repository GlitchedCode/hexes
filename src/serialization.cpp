#include <hexes/serialization.hpp>

// Template implementations live in the header (serialization.hpp).
// This translation unit exists to anchor the hexes_lib target and can be
// extended with non-template serialization helpers as needed.
